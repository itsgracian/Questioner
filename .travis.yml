language: node_js
node_js:
  - "stable"
script:
  - npm test

after_success:
  -npm run coveralls
  services:
  - postgresql
  before_script:
   - psql -c 'create database questionertest;' -U postgres
